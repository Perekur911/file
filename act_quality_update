Да, это меняется в `create_quality_act.py`. Сейчас имя создаётся слишком рано и выглядит так:

```python
today_str = datetime.today().strftime("%d-%m-%Y, %H-%M")
new_act_path = output_dir_path / f"Акт качества {today_str}.xlsx"
```

А `project_str` у тебя собирается ниже из `Project`, `Field`, `Well`. Нужно просто **создавать имя файла после чтения `All_samples/Append1`**, когда уже известны `project`, `field`, `well`. 

### 1. Добавь импорт

Вверху `create_quality_act.py` добавь:

```python
import re
```

---

### 2. Добавь helper рядом с `normalize_text`

```python
INVALID_FILENAME_CHARS_RE = re.compile(r'[<>:"/\\|?*\x00-\x1f]')


def make_safe_filename_part(value: Any, fallback: str = "Проект") -> str:
    text = normalize_text(value)

    if not text:
        text = fallback

    text = INVALID_FILENAME_CHARS_RE.sub("_", text)
    text = re.sub(r"\s+", " ", text)
    text = text.strip(" .")

    return text or fallback
```

---

### 3. Удали/перенеси раннее создание имени файла

В `create_quality_act()` убери вот этот блок из начала функции:

```python
today_str = datetime.today().strftime("%d-%m-%Y, %H-%M")
new_act_path = output_dir_path / f"Акт качества {today_str}.xlsx"

shutil.copy2(template_path, new_act_path)
```

То есть после:

```python
output_dir_path = Path(output_dir)
output_dir_path.mkdir(parents=True, exist_ok=True)
```

сразу должно идти:

```python
logging.info("[Act] Читаем текущую книгу")
```

---

### 4. После чтения `lif_rows` и `require_columns(...)` вставь создание нового имени

Найди место после:

```python
require_columns(
    lif_map,
    [
        "typeSampleShort",
        "sampleCode",
        "Project",
        "Field",
        "Well",
    ],
    "All_samples/Append1",
)
```

И сразу после него вставь:

```python
project = first_non_empty(lif_rows, lif_map, "Project")
field = first_non_empty(lif_rows, lif_map, "Field")
well = first_non_empty(lif_rows, lif_map, "Well")

if isinstance(well, float) and well.is_integer():
    well_text = str(int(well))
else:
    well_text = normalize_text(well)

project_str = f"{project}-{field}-{well_text}" if project and field and well_text else None
project_file_part = make_safe_filename_part(project_str)

today_str = datetime.today().strftime("%d-%m-%Y, %H-%M")
new_act_path = output_dir_path / f"{project_file_part}_Акт качества {today_str}.xlsx"

shutil.copy2(template_path, new_act_path)
```

После этого уже идёт:

```python
act_wb = load_workbook(new_act_path)
act_sheet = act_wb["Акт качества"]
```

---

### 5. Ниже в блоке шапки убери повторное вычисление

Ниже у тебя сейчас снова есть:

```python
project = first_non_empty(lif_rows, lif_map, "Project")
field = first_non_empty(lif_rows, lif_map, "Field")
well = first_non_empty(lif_rows, lif_map, "Well")

if isinstance(well, float) and well.is_integer():
    well_text = str(int(well))
else:
    well_text = normalize_text(well)

project_str = f"{project}-{field}-{well_text}" if project and field and well_text else None
write_defined_name_value(act_wb, "project", project_str)
```

Замени этот кусок просто на:

```python
write_defined_name_value(act_wb, "project", project_str)
```

А ниже `well` всё ещё будет доступен, потому что мы вычислили его выше:

```python
write_defined_name_value(
    act_wb,
    "Well",
    well,
)
```

---

После этого имя будет таким:

```text
26-F412-YTB-Yu475_Акт качества 21-07-2026, 10-49.xlsx
```

То есть формат:

```text
<Project>-<Field>-<Well>_Акт качества DD-MM-YYYY, HH-MM.xlsx
```
